package app;

import util.Window;

import javax.swing.*;
import java.awt.*;

public class Cameras extends Window {
    public Cameras() {
        super("Camera Views", true, new Dimension(800, 400), new Dimension());

        showWindow();
    }
}
