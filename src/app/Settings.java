package app;

import util.Window;

import javax.swing.*;
import java.awt.*;

public class Settings extends Window {
    public Settings() {
        super("Settings", false, new Dimension(600, 400), new Dimension(800, 600));

        showWindow();
    }
}
