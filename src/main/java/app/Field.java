package app;

import core.Window;

import java.awt.*;

public class Field extends Window {
    public Field() {
        super("Field Map", true, new Dimension(800, 400), new Dimension());

        showWindow();
    }
}
